import Libraries._

object MongoGenCommon extends BaseModule {
  val moduleName = "mongo-gen-common"
  val location = "./mg-common"

  val settings = Seq ()

  def project = baseProject

  lazy val libraries = Seq ()
}
