import Libraries._

object MongoGenGeneratorApi extends BaseModule {
  val moduleName = "mongo-gen-generator-api"
  val location = "./mg-generator-api"

  val settings = Seq ()

  def project = baseProject

  lazy val libraries = Seq ()
}
